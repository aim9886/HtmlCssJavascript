// using filter


const numbers = [1, 2, 3, 4, 5];

// Remove even numbers from the array
const filteredNumbers = numbers.filter(number => number % 2 !== 0);

console.log(filteredNumbers); // Output: [1, 3, 5]



// To delete elements from an array based on certain criteria in JavaScript,
// you can use various methods like filter, splice, or even a combination of slice and concat,
// depending on your requirements.


